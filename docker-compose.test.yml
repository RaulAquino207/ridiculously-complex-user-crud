name: rc-user-crud-test
services:
  app:
    container_name: rc-user-crud-test-app
    build:
      context: .
      dockerfile: Dockerfile
      target: testing
    environment:
      - NODE_ENV=testing
      - PORT=3001
    ports:
      - "3001:3001"
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('net').connect(process.env.PORT||3001,'127.0.0.1').on('connect',()=>process.exit(0)).on('error',()=>process.exit(1))\" "]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
